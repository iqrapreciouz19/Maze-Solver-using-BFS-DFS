# Maze-Solver-using-BFS-DFS
Console-based C++ maze solver. User inputs a custom 2D matrix; selects BFS (shortest path) or DFS (any valid path). Graph built via adjacency lists; parent arrays for path backtracking; visual solution marking on grid output.
